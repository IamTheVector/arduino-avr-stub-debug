node_modules/
out/
# Ignore VSIX everywhere except the committed release package (see release/*.vsix).
*.vsix
!release/*.vsix
# Local helper scripts (often contain machine-specific paths).
*.bat
.DS_Store
Thumbs.db
*.log
.vscode-test/
